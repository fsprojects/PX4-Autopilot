# Informal Specification: `math::signFromBool`

🔬 *Generated by Lean Squad automated formal verification.*

## Source

**File**: `src/lib/mathlib/math/Functions.hpp`, line 63  
**Function signature**: `inline int signFromBool(bool positive)`

## Purpose

Converts a boolean value to a sign integer: `true` maps to `+1` and `false` maps to
`-1`. This function provides a clean mapping from boolean polarity indicators to
arithmetic sign multipliers, avoiding the implicit integer conversion `(int)bool` which
returns `{0, 1}` rather than `{-1, +1}`.

## Preconditions

- None. The function accepts any `bool` value.

## Postconditions

1. **Non-zero**: `signFromBool(b) ≠ 0` for all `b : Bool`
2. **True → positive**: `signFromBool(true) = 1`
3. **False → negative**: `signFromBool(false) = -1`
4. **Magnitude one**: `|signFromBool(b)| = 1` for all `b : Bool`
5. **Square is one**: `signFromBool(b) * signFromBool(b) = 1` for all `b : Bool`
6. **Involution-like**: `signFromBool(!b) = -signFromBool(b)` (flipping the boolean
   negates the sign)
7. **Idempotent recovery**: `signFromBool(signFromBool(b) > 0) = signFromBool(b)` — the
   original boolean can be recovered from the sign

## Invariants

- The range is exactly `{-1, 1}` — no other values are possible.
- The function is a bijection between `Bool` and `{-1, 1}`.

## Edge Cases

- No edge cases: the input domain is `{true, false}`, both covered by the two branches.

## Examples

| Input | Output |
|-------|--------|
| `true` | `1` |
| `false` | `-1` |

## Inferred Intent

This function serves as a type-safe alternative to the common C++ idiom
`2 * (int)condition - 1` or `condition ? 1 : -1` written inline. By giving it a named
function, call sites are more readable and the contract (always ±1, never 0) is explicit.

The non-zero postcondition is the most safety-relevant: callers that use the result as
a multiplier or divisor need to know it will never produce a zero.

## Open Questions

None. The implementation is trivially transparent and matches the specification exactly.

## Lean 4 Approach

All five postconditions above are decidable for finite `Bool` inputs. Every theorem
should be closeable by `decide` or `by cases b`. Estimated: ~5 theorems, 0 sorry.

Key Lean representations:
- `signFromBool (b : Bool) : Int := if b then 1 else -1`
- All theorems: `by cases b <;> decide` or `by rcases Bool.eq_false_or_eq_true b`
