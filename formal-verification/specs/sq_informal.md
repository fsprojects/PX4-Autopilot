# Informal Specification: `math::sq`

🔬 *Generated by Lean Squad automated formal verification.*

## Source

**File**: `src/lib/mathlib/math/Functions.hpp`, line 68  
**Function signature**: `template<typename T> T sq(T val)`

## Purpose

Computes the square of a value: `sq(val) = val * val`. Provides a named function for
squaring to avoid redundant evaluation of expressions (e.g., calling an expensive
computation twice) and to improve readability at call sites.

## Preconditions

- **General (`T = Rat`)**: None. Valid for all rational inputs.
- **Integer specialisation (`T = Int`)**: None. Valid for all integer inputs, but
  note that for fixed-width integers (e.g., `int16_t`), overflow can occur for large
  inputs; the Lean model uses unbounded integers and rationals.
- **Float specialisation**: The C++ `sq<float>(val)` may produce `+∞` for inputs near
  `FLT_MAX`. The Lean `Rat` model abstracts this away.

## Postconditions

1. **Non-negative**: `sq(x) ≥ 0` for all `x : Rat` (and all `x : Int`)
2. **Zero iff zero**: `sq(x) = 0 ↔ x = 0`
3. **Symmetry (even function)**: `sq(-x) = sq(x)` for all `x`
4. **Monotone on non-negatives**: `0 ≤ x ≤ y → sq(x) ≤ sq(y)` (for `Rat` and `Int`)
5. **Monotone on non-positives**: `x ≤ y ≤ 0 → sq(y) ≤ sq(x)` (symmetric case)
6. **One fixed point**: `sq(1) = 1` and `sq(-1) = 1` and `sq(0) = 0`
7. **Multiplicativity**: `sq(x * y) = sq(x) * sq(y)`

## Invariants

- `sq(x) ≥ 0` is the primary invariant — it holds for all real/rational/integer inputs.
- `sq` is an even function: symmetric under negation.

## Edge Cases

| Input | Output |
|-------|--------|
| `0` | `0` |
| `1` | `1` |
| `-1` | `1` |
| `2` | `4` |
| `-32768` (int16_t) | `1073741824` (overflows int16_t — model uses Int) |

## Examples

All verifiable by `decide` in Lean for rational inputs:
- `sq (3/2 : Rat) = 9/4`
- `sq (0 : Rat) = 0`
- `sq (-5 : Rat) = 25`

## Inferred Intent

`sq` is a utility to compute `x²` clearly at call sites. It is used throughout PX4
for computing norms, distances, and Euclidean terms in control algorithms. The
non-negativity property is the most frequently needed invariant: whenever a sum of
squares is used as a measure (e.g., norm squared, error energy), callers rely on the
non-negativity of each term.

## Open Questions

None. The function is trivially defined; the only subtlety is overflow for fixed-width
integer types, which the unbounded-integer Lean model abstracts away.

## Lean 4 Approach

- Model: `def sq (x : Rat) : Rat := x * x` (and separately for `Int`)
- Non-negativity: `Rat.mul_self_nonneg` or `mul_self_nonneg` from Lean stdlib
- Even symmetry: unfold + ring-like manipulation with `Rat.neg_mul_neg`
- Monotonicity: requires case splits on sign; omega for Int version
- Estimated: ~7 theorems, 0 sorry (all stdlib-provable)
